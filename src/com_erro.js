var multiplier = 5;
var counter = 0;
var countedList = [];
for (var position = 0; true; position = "".concat((++position) * multiplier)) {
    console.log(position);
    if (position > 100)
        break;
}
