const multiplier = 5;
let counter = 0;
const countedList = [];
for (let position = 0; true; position = `${(++position) * multiplier}`) {
    console.log(position);
    if (position > 100)
        break;
}
//# sourceMappingURL=com_erro.js.map